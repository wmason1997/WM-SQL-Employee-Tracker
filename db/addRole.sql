-- Insert example new role --
INSERT INTO role (title, salary)
VALUES 
    ("New Example Role", 99999);

SELECT * FROM role;

-- Need to also figute out how to add department it belongs to, must be the subsequent Inquirer question --